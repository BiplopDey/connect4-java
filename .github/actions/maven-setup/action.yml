# .github/actions/maven-setup/action.yml
name: 'Reusable Maven Setup'
runs:
  using: "composite"
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: 17
        distribution: temurin
        cache: maven
